# Use an official Python runtime as a parent image
FROM python:3.10.13-slim
# Set environment variables
ENV SECRET_KEY django-insecure-wflg-n%4pqru^gdr0jk5te%eu)#(s8%o-857p3okjqf041p-kb
ENV DEBUG True
ENV CELERY_BROKER_URL roundhouse.proxy.rlwy.net:15158
ENV CELERY_RESULT_BACKEND roundhouse.proxy.rlwy.net:15158

# Set the working directory in the container
WORKDIR /app

# Copy the requirements file into the container at /app
COPY requirements.txt /app/

# Install any needed packages specified in requirements.txt
RUN pip install --upgrade pip && pip install -r requirements.txt

# Copy the current directory contents into the container at /app
COPY . /app/

# Collect static files
RUN python manage.py collectstatic --noinput

# Run migrations
RUN python manage.py migrate

# Expose the port on which your Django app will run
EXPOSE 8000

# Load environment variables from .env file
ENV PATH /app:$PATH
#COPY .env /app/

# Start the application
#CMD ["python", "manage.py", "runserver", "0.0.0.0:8000"]

#
## Separate process for Celery
#CMD ["celery", "-A", "imagegenerator", "worker", "--loglevel=info"]
#CMD ["celery", "-A", "imagegenerator", "beat", "--loglevel=info"]
#
